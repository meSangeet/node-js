//to use template engines we need to have a directory with the name views. So let's say we need to change the file name views to something other.

//syntax

app.set('views', '<path of the directory>');